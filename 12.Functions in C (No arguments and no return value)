#include<stdio.h>
void add();
int main()
{
    add();
    return 0;
}
void add()
{
    int a,b,sum;
    printf("enter two numbers");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("the sum of %d and %d is %d",a,b,sum);
    getch();
}
